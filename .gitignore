# Archivos de sistema operativo y editores
.DS_Store
Thumbs.db
*.swp
*.swo
*~
desktop.ini
.idea/
.vscode/ # Si no quieres compartir la configuración específica de VS Code del proyecto

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*-debug.log
debug.log

# Dependencias (importante para Node.js)
node_modules/

# Frontend (asumiendo Vite/React, pero aplica a otros)
frontend/node_modules/
frontend/dist/ # Carpeta de build/producción del frontend
frontend/.env # Variables de entorno específicas del frontend
frontend/.env.* # Variables de entorno específicas del frontend para diferentes modos (local, development, etc.)
frontend/coverage/ # Reportes de cobertura de pruebas del frontend
frontend/.DS_Store

# Backend (Node.js)
backend/node_modules/
backend/.env # Variables de entorno del backend (IMPORTANTE para JWT_SECRET, credenciales de DB si no es Firebase Admin SDK, etc.)
backend/.env.* # Variables de entorno del backend para diferentes modos
backend/coverage/ # Reportes de cobertura de pruebas del backend
backend/config/new-digital-host-firebase-adminsdk-fbsvc-27c135e9ae.json # TU ARCHIVO ESPECÍFICO DE CREDENCIALES DE FIREBASE ADMIN SDK
backend/config/*.json # Si tienes otros archivos JSON de configuración sensibles en la carpeta config
# Si tuvieras otros archivos de credenciales o configuración sensible directamente en backend/config:
# backend/config/otra_clave_secreta.pem
backend/.DS_Store

# Archivos de build/distribución generales (si los generas en la raíz)
dist/
build/
out/
public/ # Si 'public' es una carpeta de build y no de assets estáticos que SÍ quieres subir

# Archivos de configuración de herramientas específicas
.eslintcache
.prettierignore # Si no quieres que se comparta
.stylelintcache

# Archivos de paquetes comprimidos
*.zip
*.tar
*.gz
*.rar

# Documentación generada (si no quieres subirla)
# docs/_build/

# Archivos de entornos virtuales de Python (si alguna vez usas Python en el proyecto)
venv/
*.pyc
__pycache__/
.env # General para Python si está en la raíz

# Otros archivos sensibles comunes
secrets.yml
credentials.json # General, si no está en una subcarpeta ya ignorada
*.pem
*.key